#!/usr/bin/env sh
# This file is part of Danil Kutkevich <danil@kutkevich.org> home.

. "$(cd -- "$(dirname -- "$0")" >/dev/null 2>&1 && pwd -P)/path.sh"

# "$BREWBINDIR"/scc "$@"
"$BREWBINDIR"/tokei "$@" # <https://github.com/xampprocky/tokei>.
