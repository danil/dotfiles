#!/bin/bash

# Basher is a bash/shell/functions package manager
# <https://github.com/basherpm/basher>.

command="basher install"

packages="$packages dylanaraps/neofetch"

for package in $packages; do
    $command $package
done
