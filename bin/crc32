#!/usr/bin/env bash

# This file is part of Danil Kutkevich <danil@kutkevich.org> home.

# CRC32 as unsigned decimal integer.

if [ -p /dev/stdin ]; then
    read xxx
else
    xxx="$1"
fi

printf "%s" "$xxx" | gzip -1 | tail -c 8 | head -c 4 | hexdump -e '1/4 "%08u" "\n"'