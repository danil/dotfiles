#!/usr/bin/env ruby

require 'yaml'
require 'psych'

File.open('/tmp/123', 'w') do |file|
  file.write(ARGV.inspect)
end

i18n_file, i18n_key, text = ARGV
translations = YAML.load_file(i18n_file)
translations[i18n_key] = text
File.open(i18n_file, 'w') { |f| YAML.dump(translations, f) }
