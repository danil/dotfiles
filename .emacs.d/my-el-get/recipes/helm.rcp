(:name helm
       :description "Emacs incremental and narrowing framework"
       :website "http://emacs-helm.github.io/helm"
       :type github
       :pkgname "emacs-helm/helm"
       ;; Helm has a lot of optional dependencies that would be required in
       ;; order to compile it.
       :compile nil)
