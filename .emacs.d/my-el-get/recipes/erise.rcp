(:name erise
       :description "Emacs Rails i18n translations extractor"
       :website "https://github.com/danil/erise"
       :type github
       :pkgname "danil/erise"
       :after (progn
                ;; (setq erise-key-length-limit 30)
                ;; (setq erise-shell-command "erise")
                (global-set-key (kbd "C-c d r i s") 'erise)
                )
       )
