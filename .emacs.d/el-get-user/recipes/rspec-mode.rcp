(:name rspec-mode
       :description "Enhance ruby-mode for RSpec"
       :type github
       :pkgname "pezra/rspec-mode"
       ;; :after (progn
       ;;          (setq rspec-use-rake-when-possible t))
       :features rspec-mode)
