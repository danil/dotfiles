(global-set-key (my-kbd "b h r") 'highlight-regexp)
(global-set-key (my-kbd "b u h r") 'unhighlight-regexp)
;; (defun my-highlight-regexp (&optional arg)
;;   (interactive "P")
;;   (if arg (unhighlight-regexp) (highlight-regexp)))
