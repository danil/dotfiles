(my-after-init
  (my-eval-after-load 'lisp-mode
    (define-key emacs-lisp-mode-map (my-kbd "m l b") 'my-eval-buffer)
    (define-key emacs-lisp-mode-map (my-kbd "m l r") 'my-eval-region)
    (defun my-eval-buffer ()
      (interactive)
      (eval-buffer)
      (message (format "Evaled %s" (buffer-name))))
    (defun my-eval-region (start end)
      (interactive "r")
      (eval-region start end)
      (message "Region evaled"))))
