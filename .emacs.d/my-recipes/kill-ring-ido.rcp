(my-add-to-packages 'kill-ring-ido)
;; (my-after-init (global-set-key (kbd "M-y") 'my-yank-pop-with-ido))

(defun my-yank-pop-with-ido (&optional arg)
  (interactive "P")
  (if arg
      (call-interactively 'yank-pop)
    (call-interactively 'kill-ring-ido)))
