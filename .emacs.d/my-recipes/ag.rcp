(my-after-init
  (global-set-key (kbd "C-c d f a s s") 'ag)
  (global-set-key (kbd "C-c d f a s r") 'ag-regexp)
  (global-set-key (kbd "C-c d f a f s") 'ag-dired)
  (global-set-key (kbd "C-c d f a f r") 'ag-dired-regexp)
  (global-set-key (kbd "C-c d f a k k") 'ag-kill-buffers)
  (global-set-key (kbd "C-c d f a k o") 'ag-kill-other-buffers)
  (my-hooks-with-truncate-lines 'ag-mode-hook)
  (my-eval-after-load 'ag
    ;; (cond ((equal frame-background-mode 'dark)
    ;;        ;; (set-face-background 'ag-match-face "green3")
    ;;        (set-face-foreground 'ag-match-face "lightskyblue1")))
    (setq ag-reuse-window 't)
    (set 'ag-highlight-search t)))
